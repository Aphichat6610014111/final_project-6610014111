// Transactions removed — stub to avoid dependency on transactions API
import React from 'react';
import { View } from 'react-native';

export default function TransactionScreen() {
  return <View />;
}