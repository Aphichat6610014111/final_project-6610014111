// Reports removed — stub kept to avoid navigation errors
import React from 'react';
import { View } from 'react-native';

// Reports removed — component intentionally removed. Update imports/routes if referenced.

export default null;
